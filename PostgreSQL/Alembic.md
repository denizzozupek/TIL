# Alembic (PostgreSQL Notu)

Bu konu zaten SQLAlchemy tarafinda detayli tutuluyor:

- [[../SQLAlchemy/Alembic]]

PostgreSQL baglaminda ozet:

- SQLAlchemy + Alembic ile migration yonetimi yapilir.
- `upgrade` ve `downgrade` ile surum gecisi kontrol edilir.
- Takim calismasinda schema degisikliklerini standartlastirir.